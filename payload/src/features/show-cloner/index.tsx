import { createAdminView } from '../../utils/createAdminView';
import { ShowClonerClient } from './ShowClonerClient';

export const ShowClonerTool = createAdminView(ShowClonerClient);

export default ShowClonerTool;
