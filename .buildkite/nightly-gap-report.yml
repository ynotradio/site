env:
  NODE_VERSION: "22"

steps:
  - label: "Nightly content sync retired"
    key: "nightly-content-sync-retired"
    command: |
      echo "Nightly content imports and integrity checks are retired."
      echo "This pipeline is intentionally a no-op placeholder."
    agents:
      queue: "default"
